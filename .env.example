# ─── Supabase (hosted / Vercel version) ───────────────────────────────────────
# Get these from: supabase.com → your project → Settings → API
# Set in Vercel dashboard: Project Settings → Environment Variables

VITE_SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ─── Local server Supabase sync (optional, for two-way sync) ──────────────────
# If set, Open CRM.command will sync local writes to Supabase automatically.
# Use the SERVICE ROLE key here (not the anon key) — keep this private.
# Get from: supabase.com → your project → Settings → API → service_role

SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ─── Mailchimp (Vercel env vars — for bulk sync via /api/mailchimp-sync) ──────
# Set in Vercel dashboard: Project Settings → Environment Variables
# Get from: Mailchimp → Account → Extras → API keys
#
# MAILCHIMP_API_KEY format: xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx-us8
# MAILCHIMP_AUDIENCE_ID: your Audience/List ID (Account → Audience → Settings → Audience ID)
#
# Note: the Supabase Edge Function (auto-sync on save) also needs these set as
# Supabase secrets — see setup/mailchimp-setup.md Step 4. It additionally
# requires MAILCHIMP_SERVER (e.g. "us8" — the suffix after the dash in the API key).

MAILCHIMP_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx-us8
MAILCHIMP_AUDIENCE_ID=xxxxxxxxxx

# ─── Notes ────────────────────────────────────────────────────────────────────
# Copy this file to .env.local and fill in your real values.
# .env.local is gitignored — never commit real credentials.
# Without these vars, the app runs in local-only mode (reads/writes local JSON).
